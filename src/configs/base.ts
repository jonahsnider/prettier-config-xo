import type {Options} from 'prettier';

/**
 * Base XO config.
 */
export const base: Options = {
	arrowParens: 'avoid',
	bracketSpacing: false,
	jsxSingleQuote: true,
	singleQuote: true,
	trailingComma: 'all',
	useTabs: true,
};
